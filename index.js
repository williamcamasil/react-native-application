/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Home from './src/features/validations-app/screens/HomeScreens';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Home);
